/// White-label overrides — keep in sync with [build_config.yaml].
class BuildConfig {
  static const String appName = 'Deskward';
  static const String bundleId = 'com.deskward.app';
  static const String iconPath = 'assets/icon.png';
}
