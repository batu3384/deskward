# Faz 5.1 — VideoToolbox HW H.264

> **Goal:** macOS host uses VideoToolbox for H.264; software fallback preserved.

## Done

- `deskward-host-macos/src/vt_h264.rs` — `CompressionSession` + IOSurface BGRA upload
- `CodecProfile::H264Hw` — `DESKWARD_CODEC=h264-hw` or setup.json `"codec": "h264-hw"`
- Flutter Ayarlar → H.264 (donanım) — macOS only
- Fallback: VT fail → OpenH264 software

## Next (Faz 5.2)

- iOS VideoToolbox decode (native texture, skip JPEG bridge)
- Windows Media Foundation HW encode
