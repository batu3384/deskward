# Faz 3 — Pro seviye Implementation Plan

> **Goal:** Self-host Pro-lite: console API, ACL, multi-relay, audit, recording hooks.

**Architecture:** `deskward-console` Axum API; id server multi-relay registry; feature flags in core.

## Tasks

### Task 1: Admin API

**Files:** `deskward-console/src/main.rs`

- [ ] `GET /api/v1/devices` — list registered peers
- [ ] `POST /api/v1/groups` — ACL group stub
- [ ] `GET /health`

### Task 2: Multi-relay registry

**Files:** `deskward-id/src/relay_registry.rs`

- [ ] Config file `relays.toml` list
- [ ] Return nearest relay in `PunchResponse` extension (Faz 3.1)

### Task 3: Audit log

**Files:** `deskward-console/src/audit.rs`

- [ ] Append-only JSONL session events

### Task 4: Session recording hook

**Files:** `deskward-core/src/features/recording.rs`

- [ ] `RecordingSink` trait; no-op default

### Task 5: White-label build

**Files:** `deskward-app/build_config.yaml`

- [ ] App name, bundle id, icon path overrides

### Success

Console lists devices from id registry; audit log writes on connect; relay list configurable.
